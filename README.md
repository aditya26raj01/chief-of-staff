# Chief of Staff API

FastAPI Backend.

## Development

```bash
# Setup
uv venv
source .venv/bin/activate
uv pip install -e ".[dev]"

# Run
uvicorn app.main:app --reload

# Pre-commit
pre-commit install
```
